const cards=document.querySelector('#lessons'),ws=document.querySelector('#workspace');const completed=JSON.parse(localStorage.getItem('mcp-completed')||'[]');function render(level='all'){cards.innerHTML=LESSONS.filter(x=>level==='all'||x.level===level).map(x=>`<article class="card"><button data-i="${LESSONS.indexOf(x)}"><span class="badge">${x.level} · step ${x.step}</span><h3>${x.title} ${completed.includes(x.step)?'✓':''}</h3><p>${x.summary}</p></button></article>`).join('');document.querySelectorAll('.card button').forEach(b=>b.onclick=()=>open(+b.dataset.i))}function open(i){const x=LESSONS[i];ws.hidden=false;level.textContent=x.level.toUpperCase()+` · STEP ${x.step}`;title.textContent=x.title;summary.textContent=x.summary;learn.innerHTML=`<h3>Concept summary</h3><p>${x.detail}</p><p><a href="${x.material}" target="_blank" rel="noreferrer">Read the complete theory on GitHub →</a></p><p>Progress: ${completed.length}/${LESSONS.length} lessons completed.</p>`;lab.innerHTML=`<h3>Practical lab</h3><p>${x.detail}</p><pre>${x.run}</pre><p><a href="${x.lab}" target="_blank" rel="noreferrer">Open Python source →</a>${x.notebook?` · <a href="${x.notebook}" target="_blank" rel="noreferrer">Open notebook →</a>`:''}</p><p>No credentials are required; examples use synthetic data.</p>`;checkpoint.innerHTML=`<h3>Checkpoint</h3><p>Test the concept, then review the explanation.</p><div class="question"><strong>${x.q[0]}</strong>${x.q[1].map((a,j)=>`<label><input type="radio" name="q" value="${j}"> ${a}</label>`).join('')}<button id="check">Check answer</button><p id="feedback"></p></div>`;document.querySelector('#check').onclick=()=>{const v=document.querySelector('input[name=q]:checked');feedback.textContent=v&&+v.value===x.q[2]?`Correct — ${x.q[3]}`:`Review — ${x.q[3]}`;feedback.className='result';completed.push(x.step);localStorage.setItem('mcp-completed',JSON.stringify([...new Set(completed)]));};ws.scrollIntoView({behavior:'smooth'})}document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.level)});document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');['learn','lab','checkpoint'].forEach(id=>document.querySelector('#'+id).hidden=id!==b.dataset.tab)});render();
