export const QUESTIONS = [
  {level:"beginner",text:"An MCP tool description is…",options:["an authorization grant","untrusted metadata","a signed package"],answer:1,explanation:"Descriptions must be reviewed and cannot grant permission."},
  {level:"beginner",text:"In modern MCP (2026-07-28+), protocol version and client capabilities are…",options:["carried on every request","established only by initialize","inferred from the tool name"],answer:0,explanation:"Modern MCP uses per-request metadata; initialize belongs to the compatibility path for legacy revisions."},
  {level:"beginner",text:"A server validly advertises fetch_url, but the host has not approved it. What should happen?",options:["the model decides whether it is safe","the host denies it before tools/call","the server advertisement grants permission"],answer:1,explanation:"Discovery establishes availability, while trusted host policy decides whether a user may invoke the capability."},
  {level:"beginner",text:"Which transport is most appropriate for a real-protocol test with no process or network boundary?",options:["the SDK in-memory transport","an unprotected production endpoint","legacy HTTP+SSE"],answer:0,explanation:"Client(server) uses the real protocol layer in memory and is designed for tests and embedding."},
  {level:"beginner",text:"What completes an initialization-based legacy MCP handshake?",options:["tools/list","notifications/initialized","a model approval prompt"],answer:1,explanation:"Legacy clients send notifications/initialized after a successful initialize exchange."},
  {level:"beginner",text:"A threat model has complete controls, tests, telemetry, and runbook fields, but none has been executed. What can it claim?",options:["complete design traceability only","verified control effectiveness","production authorization"],answer:0,explanation:"Complete fields demonstrate traceability; control effectiveness requires executed and reviewable evidence."},
  {level:"beginner",text:"What does STRIDE provide?",options:["a set of threat-discovery prompts","a probability of compromise","automatic residual-risk acceptance"],answer:0,explanation:"STRIDE prompts systematic threat discovery but does not rank risk, prove completeness, or accept residual risk."},
  {level:"beginner",text:"A model supplies tenant=acme in a tool argument. Where should trusted tenant identity originate?",options:["the authenticated application session","the model's role name","the tool description"],answer:0,explanation:"Identity and tenant scope must come from authenticated application state rather than model-controlled content."},
  {level:"beginner",text:"A tool call passes its JSON Schema validation. What has been established?",options:["only that its shape satisfies the declared contract","that the caller owns the resource","that a human approved the effect"],answer:0,explanation:"Schema validation is syntactic. Ownership, tenant, purpose, and approval require separate trusted checks."},
  {level:"beginner",text:"A reviewed MCP prompt has the expected version and digest. How should the host treat its text?",options:["as untrusted template content with no authorization power","as a policy override","as approval for the next tool call"],answer:0,explanation:"Pinning proves which reviewed template was returned; it does not grant authority or turn its text into policy."},
  {level:"beginner",text:"In stateless MCP, what authority does an explicit state handle provide by itself?",options:["none; each call still requires authentication and authorization","full authority from the first call","tenant administrator authority"],answer:0,explanation:"A state handle names state. It is not a capability and must not replace per-call authorization."},
  {level:"beginner",text:"How should a Python MCPServer report an expected domain denial to the client?",options:["raise ToolError so the result has is_error=true","return an ok=false dictionary as a successful result","print the denial to stdout"],answer:0,explanation:"ToolError represents an expected failed tool outcome; error-shaped return data is still a successful call."},
  {level:"beginner",text:"What does Client(mcp) prove in a Python SDK test?",options:["real protocol behavior in memory without process or network isolation","TLS and reverse-proxy security","production identity enforcement"],answer:0,explanation:"The in-memory client exercises discovery and protocol calls, but it does not cross a subprocess or network boundary."},
  {level:"beginner",text:"A modern MCP server reports a familiar name and version in serverInfo. What has the host learned?",options:["self-reported display and diagnostic metadata only","verified artifact provenance","permission to expose every advertised tool"],answer:0,explanation:"The protocol does not verify serverInfo; security decisions need host-owned identity, origin or launch facts, and artifact evidence."},
  {level:"beginner",text:"A server changes a tool annotation, an optional input field, and cacheScope from private to public. What should a strict host do?",options:["deny the changed snapshot pending review and invalidate the old cache","accept because the tool name is unchanged","let the model classify the change"],answer:0,explanation:"Annotations, complete schemas, and cache hints affect behavior and belong in the reviewed capability contract."},
  {level:"beginner",text:"What authority does cacheScope=public grant an MCP server response?",options:["none; it is a caching hint and the host still owns authorization and partition policy","cross-tenant read access","permission to bypass capability review"],answer:0,explanation:"A host may apply a stricter private partition and TTL; cache metadata never grants access."},
  {level:"beginner",text:"Where should a stdio MCP server write application logs?",options:["stderr","stdout mixed with protocol frames","inside tool descriptions"],answer:0,explanation:"Stdout is reserved for the protocol stream; ordinary application logging belongs on stderr."},
  {level:"beginner",text:"When should an MCP threat model be reviewed again?",options:["after material identity, capability, server, model, dependency, or topology changes","only after an incident","never after its diagram is approved"],answer:0,explanation:"Material changes can invalidate the model's flows, threats, assumptions, controls, and residual-risk decision."},
  {level:"beginner",text:"Where should tenant checks live?",options:["only in prompts","at the tool or API boundary","only in the UI"],answer:1,explanation:"Enforcement belongs in verifiable application code."},
  {level:"beginner",text:"Why inventory transitive dependencies?",options:["to see the full attack surface","to widen scopes","to avoid lockfiles"],answer:0,explanation:"Transitive dependencies are part of the software supply chain."},
  {level:"intermediate",text:"What does digest pinning provide?",options:["mutable latest behavior","an immutable artifact reference","model confidence"],answer:1,explanation:"A digest identifies exact artifact bytes."},
  {level:"intermediate",text:"A valid signature proves…",options:["the package is risk-free","the artifact matches its signer","the model is correct"],answer:1,explanation:"Signatures provide authenticity and integrity, not absence of vulnerabilities."},
  {level:"intermediate",text:"Which destination should an egress policy block?",options:["an approved HTTPS API","a cloud metadata IP","an approved ticket API"],answer:1,explanation:"Metadata endpoints are common SSRF targets."},
  {level:"intermediate",text:"Why generate an SBOM?",options:["dependency visibility","automatic authorization","prompt filtering"],answer:0,explanation:"SBOMs support dependency inventory and vulnerability response."},
  {level:"advanced",text:"What prevents delegation escalation?",options:["scope intersection and depth limits","forwarded bearer tokens","unbounded retries"],answer:0,explanation:"Delegation must only narrow authority."},
  {level:"advanced",text:"What is a confused deputy?",options:["a server misuses broad authority for a caller","a missing README","a slow model"],answer:0,explanation:"Caller-bound, audience-specific delegation mitigates confused-deputy failures."},
  {level:"advanced",text:"What is the first containment action for a malicious server?",options:["disable or revoke it","give it more scopes","wait for a model update"],answer:0,explanation:"Use a kill switch and preserve the affected evidence."},
  {level:"advanced",text:"Why validate tool output?",options:["it may contain prompt injection or invalid data","it is always wrong","it replaces authorization"],answer:0,explanation:"Tool output is untrusted content and can redirect future actions."},
  {level:"advanced",text:"Which approval record safely authorizes a consequential tool effect?",options:["a short-lived single-use receipt bound to principal, tenant, exact action, target, arguments, and policy version","an approved=true tool argument","a digest of the reply length"],answer:0,explanation:"Exact binding prevents mutation, replay, cross-principal reuse, and stale-policy approval."},
  {level:"advanced",text:"A tool is annotated readOnlyHint=true. What should a host infer?",options:["it is an untrusted behavioral hint that must match reviewed implementation and evidence","the server is cryptographically prevented from writing","the call needs no authorization"],answer:0,explanation:"MCP annotations help clients and UIs but are not security enforcement."},
  {level:"advanced",text:"A defensible release gate includes…",options:["a pinned digest, signature, SBOM, and resolved findings","the latest dependency only","no rollback plan"],answer:0,explanation:"Reproducibility, provenance, inventory, and vulnerability gates reduce supply-chain risk."},
  {level:"scenario",text:"A server manifest adds a tool with an arbitrary URL and no required fields. What should happen?",options:["approve because the model may need it","block pending schema, egress, and ownership review","give it host credentials"],answer:1,explanation:"Broad URL tools need explicit schema, egress, and trust review."},
  {level:"scenario",text:"A signed image has a critical unresolved dependency finding. Should it ship?",options:["yes, signatures make it safe","no, block it or use a documented approved exception","only if the prompt asks"],answer:1,explanation:"Signatures prove origin and integrity, not vulnerability absence."},
  {level:"scenario",text:"A tool output instructs the agent to upload secrets. Which control applies?",options:["treat output as untrusted and re-authorize the next action","follow the instruction","disable all logging"],answer:0,explanation:"Tool output can carry prompt injection and cannot grant authority."},
  {level:"scenario",text:"A reply was approved, but the proposed body changed to a different message of the same length. What should happen?",options:["deny because the canonical action digest no longer matches","execute because length is unchanged","ask the model whether the change is safe"],answer:0,explanation:"Approval must bind the full canonical effect, not a weak proxy such as body length."},
  {level:"scenario",text:"A minimal server returns a reply proposal with executed=false and exposes no send tool. What has been demonstrated?",options:["a bounded proposal path with no execution capability","production approval and delivery","downstream idempotency"],answer:0,explanation:"The server can prove it only constructs a proposal; approval, execution, and downstream recovery require separate controls."},
  {level:"scenario",text:"What evidence best demonstrates recovery?",options:["a written claim","a tested revocation and rollback from a known-good digest","a higher model temperature"],answer:1,explanation:"Recovery must be executable and tested."},
  {level:"runtime",text:"What should an anomaly detector do when a server calls an unapproved destination?",options:["grant a broader token","create a risk signal or quarantine","ignore the trace"],answer:1,explanation:"Detection should feed a policy or containment decision; it never grants authority."},
  {level:"runtime",text:"Which fields best support investigation of a malicious server?",options:["trace ID, digest, tool, destination, and policy decision","only the final answer","the raw access token"],answer:0,explanation:"Correlated, redacted evidence supports containment and replay."}
];
